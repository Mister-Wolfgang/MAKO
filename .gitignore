# Memory database (local per-machine, SQLite-Vec)
memory/

# Legacy SHODH files (no longer used)
servers/shodh-memory/

# MCP config -- tracked for plugin distribution (hook patches command dynamically)
# .mcp.json is NOT ignored: it must ship with the plugin

# Node.js dependencies (test infrastructure)
node_modules/

# Coverage reports
coverage/
